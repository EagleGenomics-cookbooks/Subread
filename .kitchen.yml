---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.5

suites:
  - name: default
    run_list:
      - recipe[Subread]
    attributes:
  - name: subread_version
    run_list:
      - recipe[Subread]
    attributes:
      Subread:
        version: 1.4.5-p1