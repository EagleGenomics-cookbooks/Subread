---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.6

suites:
  - name: default
    run_list:
      - recipe[Subread]
    attributes:
  - name: subread_version
    run_list:
      - recipe[Subread]
    attributes:
      Subread:
        version: 1.4.6-p5
